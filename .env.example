# ============================================================
#  MCP WebScraper - Environment Variables
# ============================================================

# Transport: "sse" for network (deploy), "stdio" for local (Claude Desktop)
MCP_TRANSPORT=sse

# SSE server host and port
MCP_HOST=0.0.0.0
MCP_PORT=8000
